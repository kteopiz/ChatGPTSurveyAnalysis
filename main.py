from graphProduction import *

# make a menu pls, it will make accessing and calling functions so much easier
# API type shit.
# In order to loop the menu -> might have to return a different value than "Display Closed"
